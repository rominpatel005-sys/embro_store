"use client";
import { FormEvent,useState } from "react"; import { useRouter } from "next/navigation"; import { api,Order } from "@/lib/api";
export default function Checkout(){const router=useRouter();const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));try{const o=await api<Order>("/checkout/",{method:"POST",body:JSON.stringify(data)});router.push(`/orders/${o.id}`)}catch(e){setError((e as Error).message)}}const fields=["full_name","mobile","email","address","state","city","pincode"];
return <div className="mx-auto max-w-2xl px-5 py-12"><h1 className="text-4xl font-black">Checkout</h1><form onSubmit={submit} className="mt-8 grid gap-4 sm:grid-cols-2">{fields.map(f=><input key={f} name={f} required type={f==="email"?"email":"text"} placeholder={f.replace("_"," ")} className={`${f==="address"?"sm:col-span-2":""} rounded-xl bg-zinc-900 p-4`}/>)}<select name="payment_method" className="rounded-xl bg-zinc-900 p-4 sm:col-span-2"><option value="COD">Cash on delivery</option><option value="UPI">UPI (gateway integration required)</option></select>{error&&<p className="text-red-400 sm:col-span-2">{error}</p>}<button className="rounded-xl bg-amber-400 p-4 font-bold text-black sm:col-span-2">Place order</button></form></div>}
