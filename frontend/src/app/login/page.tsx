"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { FormEvent,useState } from "react"; import { useAuth } from "@/components/AuthProvider";
export default function Login(){const {login}=useAuth();const router=useRouter();const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);try{await login(String(d.get("username")),String(d.get("password")));router.push("/")}catch{setError("Invalid username or password.")}}
return <div className="mx-auto max-w-md px-5 py-20"><h1 className="text-4xl font-black">Welcome back</h1><form onSubmit={submit} className="mt-8 space-y-4"><input name="username" required placeholder="Username" className="w-full rounded-xl bg-zinc-900 p-4"/><input name="password" type="password" required placeholder="Password" className="w-full rounded-xl bg-zinc-900 p-4"/>{error&&<p className="text-red-400">{error}</p>}<button className="w-full rounded-xl bg-amber-400 p-4 font-bold text-black">Sign in</button></form><p className="mt-5 text-zinc-400">New here? <Link href="/register" className="text-amber-400">Create an account</Link></p></div>}
