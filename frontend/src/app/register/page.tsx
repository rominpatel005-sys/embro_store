"use client";
import { FormEvent,useState } from "react"; import { useRouter } from "next/navigation"; import { api } from "@/lib/api";
export default function Register(){const router=useRouter();const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));try{const x=await api<{access:string;refresh:string}>("/auth/register/",{method:"POST",body:JSON.stringify(d)});localStorage.setItem("access",x.access);localStorage.setItem("refresh",x.refresh);router.push("/");router.refresh()}catch(e){setError((e as Error).message)}}
return <div className="mx-auto max-w-xl px-5 py-16"><h1 className="text-4xl font-black">Create account</h1><form onSubmit={submit} className="mt-8 grid gap-4 sm:grid-cols-2"><input name="first_name" required placeholder="First name" className="rounded-xl bg-zinc-900 p-4"/><input name="last_name" required placeholder="Last name" className="rounded-xl bg-zinc-900 p-4"/><input name="username" required placeholder="Username" className="rounded-xl bg-zinc-900 p-4"/><input name="email" type="email" required placeholder="Email" className="rounded-xl bg-zinc-900 p-4"/><input name="mobile" required placeholder="Mobile" className="rounded-xl bg-zinc-900 p-4"/><input name="password" type="password" required placeholder="Password" className="rounded-xl bg-zinc-900 p-4"/>{error&&<p className="text-red-400 sm:col-span-2">{error}</p>}<button className="rounded-xl bg-amber-400 p-4 font-bold text-black sm:col-span-2">Create account</button></form></div>}
