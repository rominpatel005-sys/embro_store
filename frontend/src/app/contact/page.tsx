"use client";
import { FormEvent,useState } from "react";import { api } from "@/lib/api";
export default function Contact(){const [done,setDone]=useState(false);const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();try{await api("/contact/",{method:"POST",body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});setDone(true)}catch(e){setError((e as Error).message)}}return <div className="mx-auto max-w-xl px-5 py-16"><h1 className="text-4xl font-black">Talk to us</h1>{done?<p className="mt-8 text-green-400">Thanks—your message has been received.</p>:<form onSubmit={submit} className="mt-8 space-y-4">{["name","email","subject"].map(x=><input required type={x==="email"?"email":"text"} name={x} placeholder={x} key={x} className="w-full rounded-xl bg-zinc-900 p-4"/>)}<textarea required name="message" placeholder="message" rows={6} className="w-full rounded-xl bg-zinc-900 p-4"/>{error&&<p className="text-red-400">{error}</p>}<button className="w-full rounded-xl bg-amber-400 p-4 font-bold text-black">Send message</button></form>}</div>}
